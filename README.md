# MapMap - 3D Earth Globe

一个基于 Three.js 和 React + TypeScript 的交互式 3D 地球应用。

## 特性

- ✨ 使用 React Three Fiber 构建的 3D 地球
- 🌍 深蓝色地球外观，带星空背景
- 🖱️ 完整的鼠标交互支持
  - 左键拖动：旋转地球
  - 滚轮：缩放视图
  - 右键拖动：平移视图
- 🔄 地球自动缓慢自转
- 🎨 现代化的 UI 设计

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具
- **Three.js** - 3D 图形库
- **@react-three/fiber** - React 的 Three.js 渲染器
- **@react-three/drei** - Three.js 辅助组件库

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173` 启动。

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

### 预览生产构建

```bash
npm run preview
```

## 项目结构

```
mapmap/
├── src/
│   ├── components/
│   │   └── Globe.tsx          # 3D 地球组件
│   ├── App.tsx                # 主应用组件
│   ├── App.css                # 应用样式
│   ├── main.tsx               # 应用入口
│   └── index.css              # 全局样式
├── public/                    # 静态资源
├── index.html                 # HTML 模板
├── vite.config.ts             # Vite 配置
├── tsconfig.json              # TypeScript 配置
└── package.json               # 项目配置
```

## 未来计划

- 📦 支持加载 SHP 文件
- 📍 支持加载 GeoJSON 文件
- 🗺️ 在地球上渲染地理数据
- 🎯 添加数据可视化功能

## 开发说明

### 组件说明

- **Globe.tsx**: 核心地球组件，包含球体几何体和材质设置
- **App.tsx**: 主应用组件，设置 Canvas、相机、灯光和控制器
- **OrbitControls**: 提供鼠标交互控制
- **Stars**: 添加星空背景效果

### 自定义地球外观

在 `src/components/Globe.tsx` 中修改材质属性：

```tsx
<meshStandardMaterial
  color="#1e3a8a"        // 主颜色
  roughness={0.7}        // 粗糙度 (0-1)
  metalness={0.3}        // 金属感 (0-1)
  emissive="#0c1f47"     // 自发光颜色
  emissiveIntensity={0.2} // 自发光强度
/>
```

## License

MIT
